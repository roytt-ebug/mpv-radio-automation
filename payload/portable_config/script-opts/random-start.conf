# Restart MPV after changing these settings.
section_mode=yes
min_duration_minutes=15
section_min_minutes=10
section_max_minutes=30
# Fade out the current sample, then load the next track. No overlap.
# Set to 0 for ordinary track changes without a fade.
fade_seconds=5
